{
  "name": "bulk-url-health-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Distributed bulk URL health checker built with Fastify, BullMQ, Redis, PostgreSQL, and Next.js",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently -n \"api,worker,web\" -c \"blue,green,magenta\" \"npm run dev -w @healthchecker/api\" \"npm run dev -w @healthchecker/worker\" \"npm run dev -w @healthchecker/web\"",
    "dev:api": "npm run dev -w @healthchecker/api",
    "dev:worker": "npm run dev -w @healthchecker/worker",
    "dev:web": "npm run dev -w @healthchecker/web",
    "test": "npm run test --workspaces --if-present",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
